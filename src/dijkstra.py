from graph import Graph


{1: {2: 400}, 2: {3: 500}, 3: {1: 600}}

def djkstra(graph, start, finish):
    visited = {}
    shortest_path = float('inf')

    current = start
    nodes = graph.nodes()   # a list of nodes
    total_length = 0
    while nodes:
        route = currrnt.dft()
        for node in route:

        for edge in current:
            length += current[edge]


    return shortest_path
